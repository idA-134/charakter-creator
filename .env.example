# Beispiel Umgebungsvariablen
# Kopieren Sie diese Datei und benennen Sie sie um:
# - backend/.env für Backend Konfiguration
# - frontend/.env für Frontend Konfiguration

# ================================
# BACKEND (.env im backend/ Ordner)
# ================================

# Database
DATABASE_URL=./database.sqlite

# Server Configuration
PORT=3000
NODE_ENV=development
# NODE_ENV=production

# JWT Authentication
# Generieren Sie einen sicheren Secret mit: openssl rand -base64 32
JWT_SECRET=ihr-sehr-geheimer-jwt-schluessel-hier

# CORS Configuration
# Komma-getrennte Liste erlaubter Origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=info
# Optionen: error, warn, info, debug

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ================================
# FRONTEND (.env im frontend/ Ordner)
# ================================

# API Base URL
VITE_API_URL=http://localhost:3000

# Optional: Weitere Frontend Konfiguration
# VITE_APP_NAME=Charakter Creation
# VITE_ENABLE_ANALYTICS=false
