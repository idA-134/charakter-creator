# Dateien und Verzeichnisse die VOR dem Server-Upload gelöscht werden können

# Node modules (werden auf dem Server neu installiert)
backend/node_modules/
frontend/node_modules/
node_modules/

# Build Ausgaben (werden auf dem Server neu gebaut)
backend/dist/
frontend/dist/
frontend/build/

# Environment Dateien (werden auf dem Server neu erstellt)
backend/.env
frontend/.env
.env

# SQLite Datenbank (wird auf dem Server neu initialisiert)
backend/database.sqlite
backend/database.sqlite-journal
backend/*.db
backend/*.sqlite

# IDE spezifische Dateien
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temporäre Dateien
tmp/
temp/
*.tmp

# OS spezifisch
Thumbs.db
.DS_Store

# Optional: Entwicklungsdokumentation (wenn nicht benötigt)
# README.md
# SETUP.md
# .git/
